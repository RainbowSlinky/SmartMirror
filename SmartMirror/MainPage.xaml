<Page
    x:Class="SmartMirror.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartMirror"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:SmartMirror"
    xmlns:GmailView="using:SmartMirror.Messenger_Notification.Google"
    xmlns:GmailViewModel="using:SmartMirror.Messenger_Notification.Google"
    mc:Ignorable="d">
    
    <UserControl.DataContext>
        <ViewModel:MainPage_ViewModel></ViewModel:MainPage_ViewModel>
    </UserControl.DataContext>

    <Page.Resources>
        <DataTemplate x:Key="gmail_Template" x:DataType="GmailViewModel:Gmail_ViewModel">
            <GmailView:Gmail_View/>
        </DataTemplate>
    </Page.Resources>
        
    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" RequestedTheme="Dark">
        <ContentControl Content="{Binding Gmail_Module}" ContentTemplate="{StaticResource gmail_Template}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="100" Height="100"/>
        <Button x:Name="button" Content="Button" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="button_Click"/>
<Button Background="{Binding indicator}" Height="24" Width="24"/>
    </StackPanel>
</Page>
